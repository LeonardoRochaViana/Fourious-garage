"use client";
import { useEffect, useState } from "react";
import { Sparkles } from "lucide-react";
type Product={id:string;name:string;description:string;purpose:string};
export default function Products(){const[products,setProducts]=useState<Product[]>([]);useEffect(()=>{fetch("/api/catalog").then(r=>r.json()).then(d=>setProducts(d.products||[])).catch(()=>{})},[]);
return <section className="border-b border-garage-line bg-black/25 py-20"><div className="section-shell"><div className="mb-12 max-w-2xl"><p className="text-sm font-bold uppercase text-garage-red">Produtos utilizados</p><h2 className="red-line mt-2 font-display text-4xl font-bold uppercase sm:text-5xl">Química profissional em cada etapa</h2><p className="mt-7 text-garage-text">Produtos selecionados e cadastrados pela Furious Garage.</p></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{products.map(p=><article key={p.id} className="border border-garage-line bg-garage-panel p-5"><div className="mb-5 flex h-11 w-11 items-center justify-center bg-red-950/60 text-garage-red"><Sparkles size={22}/></div><h3 className="font-display text-2xl font-bold uppercase">{p.name}</h3><p className="mt-3 text-sm leading-6 text-garage-text">{p.description}</p><p className="mt-5 border-t border-garage-line pt-4 text-sm font-bold">{p.purpose}</p></article>)}</div></div></section>}
